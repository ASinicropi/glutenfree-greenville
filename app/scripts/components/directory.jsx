var React = require('react');
var $ = require('jquery');
var Backbone = require('backbone');

var Restaurant = require ('../models/directory.js').Restaurant;
var RestaurantCollection = require ('../models/directory.js').RestaurantCollection;
var parseHeaders = require('../parseUtilities.js').parseHeaders;
